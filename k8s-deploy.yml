---
- hosts: 192.168.1.222
  tasks:
  
  - name: delete the deployment
    become: true
    shell: kubectl delete deploy webapp
    ignore_errors: yes

  - name: create the deployment
    become: true
    shell: kubectl apply -f /root/demo/deployment.yaml